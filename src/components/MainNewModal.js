import React from 'react'
import { Modal } from '@material-ui/core'

export const MainNewModal = ({open}) => {





    return (
        <Modal open={open}>
            <div style={{display:'flex',backgroundColor:'white'}}>
                <p>hello</p>
            </div>
        </Modal>
    )
}